import { Routes } from '@angular/router';
import { InfiniteScrollPage } from './pages/infinite-scroll/infinite-scroll.component';

export const routes: Routes = [
  { path: 'infinite-scroll', component: InfiniteScrollPage }
];
